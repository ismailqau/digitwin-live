{
  "name": "@clone/tts-service",
  "version": "1.0.0",
  "private": true,
  "description": "Text-to-Speech service with voice cloning (XTTS-v2, Google, OpenAI)",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "jest",
    "lint": "eslint . --ext .ts",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@clone/config": "workspace:*",
    "@clone/errors": "workspace:*",
    "@clone/logger": "workspace:*",
    "@clone/shared-types": "workspace:*",
    "@google-cloud/text-to-speech": "^5.0.0",
    "openai": "^4.20.0"
  },
  "devDependencies": {
    "@types/node": "^24.9.2",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
