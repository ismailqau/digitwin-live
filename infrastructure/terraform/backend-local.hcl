# Local backend configuration for testing
# Usage: terraform init -backend-config=backend-local.hcl
# Note: This uses local state file instead of GCS

# No configuration needed - will use local backend
